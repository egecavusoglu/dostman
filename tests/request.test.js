const Request = require('../src/request');

// Construct Request: correct chunk

// Construct Request: chunk missing either @url, @desc, @header, @body

// Execute Request: existing endpoint

// Execute Request: none existent endpoint
