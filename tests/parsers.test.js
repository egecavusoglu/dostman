const { parseDecorator, extractVariables } = require('../src/lib/parsers');

// Parse decorator

// Parse decorator: white absurd spaces

// Parse decorator: white absurd new lines.

// Parse decorator: with no decorator present.

// Extract variable: standart.

// Extract variable: function.
